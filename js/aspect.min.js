function reCaptchaLoaded() {
    var a = $(".dark-mode-checkbox")
      , e = a.data("toggler");
    e ? e() : a.data("loaded", !0),
    reCaptchaLoaded = void 0
}
$(function() {
    var a = "aspect"
      , e = parseInt(Cookies.get(a) || 0, 10) || 0
      , t = $(".dark-mode-checkbox")
      , r = !1;
    function d(d, o) {
        (void 0 === d || t.data("loaded")) && (d = void 0 === d ? e < 0 : d,
        t.removeData(),
        r = !0);
        var c = $("body")
          , i = $("div.g-recaptcha");
        d ? (c.addClass("dark"),
        i.attr("data-theme", "dark"),
        e = -1) : (c.removeClass("dark"),
        i.attr("data-theme", "light"),
        e = 1);
        r && i.each(function() {
            var a = $(this)
              , e = $("<div />").insertAfter(this)
              , t = function(a, e) {
                var t = {};
                for (var r of a[0].attributes)
                    /^data-/.test(r.name) && (t[r.name.substr(5)] = r.value),
                    e && e.attr(r.name, r.value);
                return t
            }(a, e);
            a.remove(),
            grecaptcha.render(e[0], t)
        }),
        o && Cookies.set(a, "" + e, {
            expires: 365
        })
    }
    var o = window.matchMedia("(prefers-color-scheme: dark)");
    function c(a, e) {
        e = void 0 === e || e,
        a.matches ? d(!0, e) : d(!1, e)
    }
    o.addListener(c);
    var i = $("#checkbox");
    e > 0 ? (d(!1),
    i.prop("checked", !0)) : e < 0 ? (d(!0),
    i.prop("checked", !1)) : c(o, !1),
    t.on("click", function() {
        d(e >= 0, !0)
    }).data("toggler", d)
});
